<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.myway.angular.sandbox</groupId>
    <artifactId>uppercase-app</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>uppercase-app</name>
    <description>Single deployable: http4s backend serving its own Angular frontend</description>

    <properties>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <scala.version>3.3.8</scala.version>
        <scala.binary.version>3</scala.binary.version>

        <fs2.version>3.13.0</fs2.version>

        <cats.version>2.13.0</cats.version>
        <cats-effect.version>3.7.0</cats-effect.version>

        <http4s.version>0.23.34</http4s.version>
        <logback.version>1.5.34</logback.version>
        <monocle.version>3.3.0</monocle.version>
        <ip4s.version>3.8.0</ip4s.version>

        <scala-maven-plugin.version>4.9.10</scala-maven-plugin.version>
        <maven-assembly-plugin.version>3.8.0</maven-assembly-plugin.version>
        <maven-resources-plugin.version>3.3.1</maven-resources-plugin.version>
        <frontend-maven-plugin.version>1.15.0</frontend-maven-plugin.version>

        <frontend.dir>frontend</frontend.dir>
        <frontend.dist>${frontend.dir}/dist/uppercase-frontend/browser</frontend.dist>

        <scalatest.version>3.2.20</scalatest.version>
        <munit-cats-effect.version>2.2.0</munit-cats-effect.version>
        <circe.version>0.14.15</circe.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala3-library_3</artifactId>
            <version>${scala.version}</version>
        </dependency>


        <!-- ========================================================= -->
        <!-- Cats                                                       -->
        <!-- ========================================================= -->

        <dependency>
            <groupId>org.typelevel</groupId>
            <artifactId>cats-core_${scala.binary.version}</artifactId>
            <version>${cats.version}</version>
        </dependency>

        <dependency>
            <groupId>org.typelevel</groupId>
            <artifactId>cats-free_${scala.binary.version}</artifactId>
            <version>${cats.version}</version>
        </dependency>


        <!-- ========================================================= -->
        <!-- Cats Effect                                                -->
        <!-- ========================================================= -->

        <dependency>
            <groupId>org.typelevel</groupId>
            <artifactId>cats-effect_${scala.binary.version}</artifactId>
            <version>${cats-effect.version}</version>
        </dependency>


        <!-- ========================================================= -->
        <!-- FS2                                                         -->
        <!-- ========================================================= -->

        <dependency>
            <groupId>co.fs2</groupId>
            <artifactId>fs2-core_${scala.binary.version}</artifactId>
            <version>${fs2.version}</version>
        </dependency>

        <dependency>
            <groupId>co.fs2</groupId>
            <artifactId>fs2-io_${scala.binary.version}</artifactId>
            <version>${fs2.version}</version>
        </dependency>
        <!-- http4s server + dsl -->
        <dependency>
            <groupId>org.http4s</groupId>
            <artifactId>http4s-ember-server_${scala.binary.version}</artifactId>
            <version>${http4s.version}</version>
        </dependency>
        <dependency>
            <groupId>org.http4s</groupId>
            <artifactId>http4s-dsl_${scala.binary.version}</artifactId>
            <version>${http4s.version}</version>
        </dependency>
        <dependency>
            <groupId>com.comcast</groupId>
            <artifactId>ip4s-core_${scala.binary.version}</artifactId>
            <version>${ip4s.version}</version>
        </dependency>

        <!-- logging -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>
        <dependency>
            <groupId>dev.optics</groupId>
            <artifactId>monocle-macro_${scala.binary.version}</artifactId>
            <version>${monocle.version}</version>
            <scope>compile</scope>
        </dependency>
        <!-- ========================================================= -->
        <!-- Circe                                                       -->
        <!-- ========================================================= -->

        <dependency>
            <groupId>io.circe</groupId>
            <artifactId>circe-core_${scala.binary.version}</artifactId>
            <version>${circe.version}</version>
        </dependency>

        <dependency>
            <groupId>io.circe</groupId>
            <artifactId>circe-generic_${scala.binary.version}</artifactId>
            <version>${circe.version}</version>
        </dependency>
        <dependency>
            <groupId>org.http4s</groupId>
            <artifactId>http4s-circe_${scala.binary.version}</artifactId>
            <version>${http4s.version}</version>
        </dependency>

        <!-- ========================================================= -->
        <!-- Tests                                                       -->
        <!-- ========================================================= -->

        <dependency>
            <groupId>org.scalatest</groupId>
            <artifactId>scalatest_${scala.binary.version}</artifactId>
            <version>${scalatest.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.typelevel</groupId>
            <artifactId>munit-cats-effect_${scala.binary.version}</artifactId>
            <version>${munit-cats-effect.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.typelevel</groupId>
            <artifactId>cats-effect-testkit_${scala.binary.version}</artifactId>
            <version>${cats-effect.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.http4s</groupId>
            <artifactId>http4s-ember-client_${scala.binary.version}</artifactId>
            <version>${http4s.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <sourceDirectory>src/main/scala</sourceDirectory>

        <plugins>

            <!-- Copies the built Angular app onto the classpath so http4s can serve it as resources. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>${maven-resources-plugin.version}</version>
                <executions>
                    <execution>
                        <id>copy-frontend-build</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}/webapp</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${frontend.dist}</directory>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Compiles Scala sources -->
            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
                <version>${scala-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>scala-compile</id>
                        <goals>
                            <goal>compile</goal>
                            <goal>testCompile</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <scalaVersion>${scala.version}</scalaVersion>
                </configuration>
            </plugin>

            <!-- Builds a runnable fat jar: java -jar target/uppercase-app-1.0.0-jar-with-dependencies.jar -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>${maven-assembly-plugin.version}</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.myway.angular.sandbox.main.Main</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>


            <!-- ===================================================== -->
            <!-- Spotless / Scalafmt                                   -->
            <!-- ===================================================== -->

            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.43.0</version>

                <configuration>
                    <scala>
                        <includes>
                            <include>src/**/*.scala</include>
                        </includes>

                        <scalafmt>
                            <version>3.8.2</version>
                            <file>${project.basedir}/.scalafmt.conf</file>
                        </scalafmt>
                    </scala>
                </configuration>

                <executions>
                    <execution>
                        <id>spotless-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>apply</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>


            <!-- ===================================================== -->
            <!-- ScalaTest                                              -->
            <!-- ===================================================== -->

            <plugin>
                <groupId>org.scalatest</groupId>
                <artifactId>scalatest-maven-plugin</artifactId>
                <version>2.2.0</version>

                <configuration>
                    <reportsDirectory>
                        ${project.build.directory}/surefire-reports
                    </reportsDirectory>

                    <junitxml>.</junitxml>
                </configuration>

                <executions>
                    <execution>
                        <id>scalatest-test</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>